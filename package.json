
{
  "name": "netpulse-pro",
  "version": "3.1.0",
  "description": "Aggressive Performance Network Diagnostic Terminal",
  "author": "Network Admin",
  "main": "main.js",
  "scripts": {
    "build": "esbuild index.tsx --bundle --minify --outfile=dist/bundle.js --platform=browser --define:process.env.API_KEY='\"'${API_KEY}'\"'",
    "start": "npm run build && electron .",
    "dist": "npm run build && electron-builder --windows"
  },
  "devDependencies": {
    "electron": "^25.0.0",
    "electron-builder": "^24.0.0",
    "esbuild": "^0.19.0"
  },
  "build": {
    "appId": "com.netpulse.pro",
    "productName": "NetPulsePro",
    "win": {
      "target": [
        "portable"
      ],
      "requestedExecutionLevel": "requireAdministrator"
    },
    "directories": {
      "output": "dist_exe"
    },
    "files": [
      "dist/bundle.js",
      "index.html",
      "main.js",
      "package.json"
    ]
  }
}
